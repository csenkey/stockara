# Stockara Frontend

React/Vite static frontend for Stockara Phase 1.

The app reads generated static artifacts from:

- `/top-picks/latest.json`
- `/sell-alerts/latest.json`

Useful commands:

```bash
npm ci
npm run lint
npm run build
npm run dev
```
